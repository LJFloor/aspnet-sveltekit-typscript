// Make sure Server Side Rendering is disabled
export const ssr = false;

// If you don't use url slugs, this is faster. It reduces flickering when loading the page. Uncomment this line lines, and comment the line above.
// export const prerender = true;
